# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
    . /etc/bashrc
fi

# AWS settings to keep S3 tools happy
export AWS_DEFAULT_REGION=<%= @region %>
export AWS_ACCESS_KEY_ID=<%= @access_key %>
export AWS_SECRET_ACCESS_KEY=<%= @secret_key %>
